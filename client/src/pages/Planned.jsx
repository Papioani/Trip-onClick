import React from "react"
/* import './Planned.css' */

export default function Planned(props) {
    return (
      <div>Where to so far</div>
    )
  }

  // props.places.map(s => (
   // <li key={s.id}>
  // should take props from the ManiPage 